# Посібник користувача — VRL Client

## 📥 Встановлення

### Крок 1: Завантажьте exe

Перейдіть на сторінку [Releases](https://github.com/Hyrol7/vrl-client/releases) та завантажте `vrl_client.exe` для своєї версії.

Або завантажте напряму останню версію:
```bash
# Windows PowerShell
$url = "https://github.com/Hyrol7/vrl-client/releases/download/v0.1.0/vrl_client.exe"
Invoke-WebRequest -Uri $url -OutFile vrl_client.exe
```

### Крок 2: Переміщення на робочий стіл або в папку

Рекомендується помістити файл у окрему папку:

```
C:\Users\YourName\VRL_Client\
├── vrl_client.exe
└── config.yaml  (буде створено автоматично)
```

## ⚙️ Конфігурація

### Перший запуск

1. **Подвійний клік** на `vrl_client.exe`
2. При першому запуску програма **автоматично** створить `config.yaml`
3. Може буту помилка: 
   ```
   ERROR: Файл порожній
   ```
   Це нормально — просто закрийте програму

### Редагування конфігурації

Відкрийте `config.yaml` в текстовому редакторі (Notepad):

```yaml
app:
  name: VRL Client
  version: 0.1.0

decoder:
  host: 127.0.0.1        # ← IP адреса комп'ютера з декодером
  port: 31003            # ← Порт декодера (зазвичай 31003)
  executable: ""         # ← Шлях до decoder.exe (якщо потрібно запускати)

api:
  url: http://your-api.com/api/flights    # ← Адреса вашого серверу
  client_id: 1           # ← Ваш ID клієнта
  secret_key: your-secret-key             # ← Секретний ключ
  bearer_token: your-bearer-token         # ← Bearer токен
```

### Приклади конфігурації

#### Локальний декодер (на тому ж комп'ютері)

```yaml
decoder:
  host: 127.0.0.1
  port: 31003
  executable: C:\path\to\decoder.exe  # Повний шлях до exe
```

#### Декодер на іншому комп'ютері в локальній мережі

```yaml
decoder:
  host: 192.168.1.100    # IP адреса комп'ютера з декодером
  port: 31003
```

#### API на локальному серверу

```yaml
api:
  url: http://localhost:8000/api/flights
  client_id: 1
  secret_key: test-key
```

#### API на віддаленому серверу

```yaml
api:
  url: https://your-domain.com/api/flights
  client_id: 1
  secret_key: your-actual-secret-key
  bearer_token: eyJhbGciOiJIUzI1NiIs...
```

## 🚀 Запуск

### Спосіб 1: Через Провідник (easiest)

1. Відкрийте папку з `vrl_client.exe`
2. **Подвійний клік** на файл
3. Програма запуститься в консолі

### Спосіб 2: Через командний рядок

```cmd
cd C:\Users\YourName\VRL_Client
vrl_client.exe
```

### Спосіб 3: Через PowerShell

```powershell
& 'C:\Users\YourName\VRL_Client\vrl_client.exe'
```

## 📝 Вікно консолі

При запуску ви побачите:

```
============================================================
ІНІЦІАЛІЗАЦІЯ VRL CLIENT
============================================================

[INFO] ════════════════════════════════════════════════════════════
[INFO] ЕТАП: ЗАВАНТАЖЕННЯ КОНФІГУРАЦІЇ
[INFO] ════════════════════════════════════════════════════════════
[INFO]   ✓ config.yaml завантажена
[INFO]   ✓ config.yaml завантажена успішно
[INFO]   ...

[INFO] ════════════════════════════════════════════════════════════
[INFO] ЕТАП: ЗАПУСК ОСНОВНИХ ЦИКЛІВ
[INFO] ════════════════════════════════════════════════════════════
[INFO]   ✓ Запущено 5 основних задач
```

Якщо все добре, програма буде виконуватися в фоні.

## ⚠️ Типові проблеми

### Помилка: "Декодер не знайдений"

```
❌ ПОМИЛКА: Декодер не знайдений
   Очікуваний шлях: C:\path\to\decoder.exe
```

**Рішення:**
- Перевірте шлях у `config.yaml`
- Переконайтесь, що шлях правильний (без помилок у написанні)
- Використовуйте повний шлях: `C:\Users\...` замість `C:\...`

### Помилка: "Не вдалось підключитись до декодера"

```
❌ Не вдалось підключитись до декодера
   Перевірте адресу та порт в config.yaml
```

**Рішення:**
- Переконайтесь, що декодер запущений
- Перевірте, що адреса та порт правильні
- Якщо декодер на іншому комп'ютері, перевірте IP адресу:
  - На комп'ютері з декодером: `ipconfig` (шукайте IPv4 Address)

### Помилка: "HTTP 401" або "Unauthorized"

```
ERROR: HTTP 401 Unauthorized
```

**Рішення:**
- Перевірте `client_id`, `secret_key` та `bearer_token` у конфігурації
- Переконайтесь, що вони точні (регіст залежить)
- Зверніться до адміністратора сервера

### Програма закривається при запуску

**Рішення:**
- Перевірте, чи немає помилок у `config.yaml`
- YAML чутливий до інтервалів (використовуйте пробіли, не Tab)
- Відкрийте файл в Notepad та перевірте синтаксис

## 🔧 Розширена налаштування

### Деактивація автоматичного запуску декодера

Якщо декодер вже запущений, встановіть:

```yaml
decoder:
  executable: ""  # Залишіть пусто
```

### Зміна інтервалів обробки

```yaml
cycles:
  parser_buffer_interval: 2    # Секунди між записами в БД
  analyser_interval: 5         # Секунди між аналізом пакетів
  sender_interval: 10          # Секунди між відправками на сервер
  batch_size: 1000            # Максимум пакетів за раз
```

### Зміна часового поясу

```yaml
app:
  timezone: Europe/Kiev        # За замовчуванням: UTC
```

Доступні часові пояси див. [IANA Time Zone Database](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)

## 📊 Перевірка статусу

### Перевірка логів

Логи записуються в БД. Для перегляду:

1. Встановіть [DB Browser for SQLite](https://sqlitebrowser.org/)
2. Відкрийте `base.db`
3. Перейдіть на вкладку `logs`
4. Переглядайте останні записи

### Перевірка записаних пакетів

В DB Browser:

1. Таблиця `packets_raw` — сирі K1/K2 пакети
2. Таблиця `status` — статус системи

## 🆘 Отримання допомоги

Якщо щось не працює:

1. **Перевірте документацію:**
   - [README.md](README.md) — загальна інформація
   - [BUILD.md](BUILD.md) — про побудову
   - [RELEASE.md](RELEASE.md) — про релізи

2. **Створіть Issue:**
   - Перейдіть на [GitHub Issues](https://github.com/Hyrol7/vrl-client/issues)
   - Напишіть опис проблеми
   - Прикріпіть `config.yaml` та логи з `base.db`

3. **Контакти:**
   - Email: support@example.com
   - Discord: [посилання на сервер]

## 🔐 Безпека

- **Храніть `secret_key` у безпеці** — не поділяйтесь ним
- **Не публікуйте `config.yaml`** з реальними ключами
- **Використовуйте HTTPS** для API (не HTTP, якщо можливо)
- **Регулярно міняйте паролі**

## 💾 Резервна копія

Рекомендується робити резервну копію `base.db`:

```bash
# Windows CMD
copy base.db base.db.backup
```

## 📞 Версія та оновлення

Для перевірки версії, відкрийте `config.yaml`:

```yaml
app:
  version: 0.1.0  # Ваша поточна версія
```

Для оновлення:
1. Завантажте новий exe з [Releases](https://github.com/Hyrol7/vrl-client/releases)
2. Замініть старий файл
3. Збережіть `config.yaml` та `base.db`

Успіхів з VRL Client! 🚀
