# 📖 Інструкція: Налаштування GitHub + PyInstaller

## 1️⃣ Підготовка репозиторію

### Крок 1.1: Ініціалізувати git (якщо ще не зроблено)

```bash
cd /Users/oleksandr/Desktop/api
git init
git config user.name "Ваше Ім'я"
git config user.email "your.email@example.com"
```

### Крок 1.2: Підготувати файли

Переконайтесь, що у вас є:
```
/Users/oleksandr/Desktop/api/
├── .github/workflows/build.yml       ✓
├── .gitignore                        ✓
├── README.md                         ✓
├── vrl_client/
│   ├── vrl.py                        ✓
│   ├── parser.py                     ✓
│   ├── analyser.py                   ✓
│   ├── sender.py                     ✓
│   ├── build_exe.py                  ✓
│   ├── requirements.txt              ✓
│   ├── config.yaml                   (автогенерується)
│   └── base.db                       (автогенерується)
└── packets/                          (старі файли)
```

### Крок 1.3: Перший commit

```bash
git add .
git commit -m "Initial commit: VRL Client v1.0.0"
```

---

## 2️⃣ Завантажити на GitHub

### Крок 2.1: Створити репозиторій на GitHub

1. Перейти на https://github.com/new
2. Назва: `vrl-client`
3. Опис: "VRL Client - Local telemetry data collector"
4. Приватний або публічний (як вам треба)
5. Натиснути "Create repository"

### Крок 2.2: Зв'язати локальний git з GitHub

```bash
git remote add origin https://github.com/YOUR_USERNAME/vrl-client.git
git branch -M main
git push -u origin main
```

Замініть `YOUR_USERNAME` на ваш GitHub username.

---

## 3️⃣ Першої ручної збірки (тестування)

### Крок 3.1: Установити PyInstaller локально

```bash
cd /Users/oleksandr/Desktop/api/vrl_client
pip install pyinstaller
```

### Крок 3.2: Запустити build скрипт

```bash
python build_exe.py
```

**Результат:**
```
vrl_client/dist/VRL_Client.exe  (приблизно 60-80 MB)
```

### Крок 3.3: Тестувати exe

```bash
./vrl_client/dist/VRL_Client.exe
```

Повинні побачити:
```
[2025-11-24 14:30:45,123] INFO: ════════════════════════════════
[2025-11-24 14:30:45,123] INFO: ЕТАП 0: ПЕРЕВІРКА ЗАЛЕЖНОСТЕЙ
...
```

---

## 4️⃣ Налаштування GitHub Actions

GitHub Actions вже налаштована в файлі `.github/workflows/build.yml`.

**Що вона робить:**
- Запускається при push tag (v1.0.0, v1.0.1, тощо)
- Встановлює Python на Windows
- Встановлює залежності
- Запускає build_exe.py
- Створює GitHub Release з exe файлом

### Крок 4.1: Перший Release

```bash
cd /Users/oleksandr/Desktop/api

# Створити tag
git tag v0.1.0

# Завантажити на GitHub
git push origin v0.1.0
```

**Що буде:**
1. GitHub Actions автоматично запуститься
2. Побудує exe на Windows
3. Створить Release з exe файлом
4. Буде видимий на https://github.com/YOUR_USERNAME/vrl-client/releases

### Крок 4.2: Перевірити сборку

1. Перейти на https://github.com/YOUR_USERNAME/vrl-client
2. Натиснути "Actions"
3. Побачити워크flow "Build and Release EXE"
4. Коли готово → "Releases" → скачати exe

---

## 5️⃣ Робочий цикл розробки

### Цикл: Розробка → Сборка → Release

**Крок 5.1: Редагувати код**

```bash
# Змінити файли (наприклад vrl.py, parser.py)
vim vrl_client/vrl.py

# Тестувати локально
python vrl_client/vrl.py
```

**Крок 5.2: Зберегти зміни**

```bash
git add vrl_client/
git commit -m "Add new feature: XYZ"
git push origin main
```

**Крок 5.3: Створити Release**

```bash
# Коли готово до релізу
git tag v1.0.1
git push origin v1.0.1
```

**Крок 5.4: GitHub Actions автоматично:**
- Збере exe
- Створить Release
- Користувачи можуть завантажити

---

## 6️⃣ Для користувачів: Оновлення

### Як користувач оновлюється до нової версії:

1. Завантажити нову версію exe з GitHub Releases
2. Запустити новий exe (старий config.yaml буде використаний)
3. Готово!

**Не потрібно:**
- Переписувати конфіг
- Переписувати БД
- Інші маніпуляції

---

## 7️⃣ Поточні файли

| Файл | Статус | Дія |
|------|--------|-----|
| vrl.py | ✅ Готовий | Завантажити на GitHub |
| parser.py | ✅ Готовий | Завантажити на GitHub |
| analyser.py | ✅ Готовий | Завантажити на GitHub |
| sender.py | ✅ Готовий | Завантажити на GitHub |
| build_exe.py | ✅ Створено | Завантажити на GitHub |
| requirements.txt | ✅ Створено | Завантажити на GitHub |
| .github/workflows/build.yml | ✅ Створено | Завантажити на GitHub |
| .gitignore | ✅ Створено | Завантажити на GitHub |
| README.md | ✅ Оновлено | Завантажити на GitHub |

---

## ✅ Чек-лист

- [ ] Перший commit: `git commit -m "Initial commit"`
- [ ] Добавити remote: `git remote add origin https://...`
- [ ] Push на GitHub: `git push -u origin main`
- [ ] Первий tag: `git tag v0.1.0 && git push origin v0.1.0`
- [ ] Перевірити GitHub Actions запустилась
- [ ] Скачати exe з Releases та протестувати
- [ ] Все працює!

---

## 🚀 Готово!

Тепер у вас є:
- ✅ Git репозиторій на GitHub
- ✅ Автоматична сборка exe через GitHub Actions
- ✅ Просте оновлення для користувачів (тільки завантажити нову версію exe)
- ✅ Контроль версій та історія змін

**Наступні кроки:**
1. Заповнити користувальницькі параметри
2. Реалізувати parser.py, analyser.py, sender.py
3. Тестування всієї системи
4. Першого Release

Удачи! 🎯
