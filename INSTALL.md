# Інструкція з установки та налаштування VRL Client

## 📥 Установка

### Варіант A: Готовий EXE (Windows)

1.  Завантажте останню версію `VRL_Client.exe` з [Releases](https://github.com/Hyrol7/vrl-client/releases).
2.  Розмістіть файл у окремій папці (наприклад, `C:\VRL_Client`).
3.  Запустіть файл. При першому запуску програма створить:
    *   `config.yaml` (файл налаштувань)
    *   `base.db` (локальна база даних)
    *   Папку `logs` (якщо налаштовано)

### Варіант B: Запуск з вихідного коду (Python)

**Вимоги:** Python 3.8+, Git.

1.  Клонуйте репозиторій:
    ```bash
    git clone https://github.com/Hyrol7/vrl-client.git
    cd vrl-client
    ```
2.  Встановіть залежності:
    ```bash
    pip install -r vrl_client/requirements.txt
    ```
3.  Запустіть клієнт:
    ```bash
    python vrl_client/vrl.py
    ```

---

## ⚙️ Налаштування (config.yaml)

Відкрийте файл `config.yaml` у будь-якому текстовому редакторі.

### 1. Підключення до декодера
Налаштуйте секцію `decoder`, щоб клієнт міг отримувати дані.

```yaml
decoder:
  host: 127.0.0.1       # IP адреса комп'ютера, де запущено декодер
  port: 31003           # TCP порт (стандартний для Mode S декодерів)
  connect_timeout: 2    # Таймаут підключення (сек)
  reconnect_delay: 3    # Пауза перед повторним підключенням (сек)
```

### 2. Налаштування API
Вкажіть адресу вашого сервера та ключі доступу.

```yaml
api:
  url: 'https://your-domain.com/api/packets'      # Endpoint для прийому пакетів
  status_url: 'https://your-domain.com/api/status' # Endpoint для статусів
  client_id: 1                                     # Ваш ID клієнта
  secret_key: 'your-secret-key'                    # Ключ для підпису (HMAC)
  bearer_token: 'your-bearer-token'                # Токен авторизації
  timeout: 30                                      # Таймаут запиту (сек)
  max_retries: 3                                   # Кількість спроб при помилці
  retry_delay: 5                                   # Пауза між спробами (сек)
```

### 3. Цикли обробки (Тюнінг)
Можна змінити частоту роботи модулів під ваші потреби.

```yaml
cycles:
  parser_buffer_interval: 2  # Як часто записувати нові пакети в БД (сек)
  analyser_interval: 5       # Як часто запускати аналіз даних (сек)
  sender_interval: 10        # Як часто відправляти дані на сервер (сек)
  batch_size: 1000           # Максимальна кількість пакетів за один раз
```

---

## ❓ Вирішення проблем

**Помилка: "Connection refused"**
*   Перевірте, чи запущено декодер.
*   Перевірте правильність IP та порту в `config.yaml`.
*   Переконайтеся, що фаєрвол не блокує з'єднання.

**Помилка: "API Error 401/403"**
*   Перевірте `client_id`, `secret_key` та `bearer_token`. Вони мають співпадати з налаштуваннями на сервері.

**Помилка: "Database locked"**
*   Це може траплятися при дуже високому навантаженні. Клієнт автоматично спробує повторити операцію.
