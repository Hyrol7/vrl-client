# Посібник контрибьютора

Дякуємо за інтерес до розвитку VRL Client!

## 🛠️ Налаштування для розробки

### 1. Fork и Clone репозиторію

```bash
git clone https://github.com/YOUR_USERNAME/vrl-client.git
cd vrl-client
git remote add upstream https://github.com/Hyrol7/vrl-client.git
```

### 2. Створення віртуального оточення

```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# або
venv\Scripts\activate     # Windows
```

### 3. Встановлення залежностей для розробки

```bash
pip install -r vrl_client/requirements.txt
pip install pytest black flake8
```

## 📋 Процес контрибуції

### 1. Створення гілки

```bash
git checkout -b feature/моя-фіча
# або
git checkout -b bugfix/мій-баг
```

### 2. Що змінювати

- ✅ Код (Python файли)
- ✅ Документація (README, MD файли)
- ✅ Тести
- ❌ НЕ змінюйте version в `initialization.py` (буде вирішено при релізі)

### 3. Кодовий стиль

```bash
# Форматування коду
black vrl_client/

# Перевірка стилю
flake8 vrl_client/
```

### 4. Тестування

```bash
# Швидкий тест
python vrl_client/test_quick.py

# З декодером (якщо є)
python vrl_client/test_core.py
```

### 5. Commit

```bash
git add .
git commit -m "feat: Описання змін"
```

**Формат повідомлення commit:**
- `feat:` — нова фіча
- `fix:` — виправлення бага
- `docs:` — документація
- `test:` — тести
- `refactor:` — рефакторинг коду

### 6. Push та Pull Request

```bash
git push origin feature/моя-фіча
```

На GitHub:
1. Перейдіть на ваш fork
2. Клацніть **Compare & pull request**
3. Напишіть опис PR
4. Submit!

## 🧪 Написання тестів

Тести повинні бути в файлах `test_*.py`:

```python
def test_parser_valid_packet():
    """Тест парсингу валідного K1 пакета"""
    from parser import parse_line
    
    line = "K1 2025-11-24 12:30:45.123 AIRBUS1 N1234 35000 FL350"
    result = parse_line(line, ":memory:")
    
    assert result is not None
    assert result['callsign'] == 'AIRBUS1'
    assert result['height'] == 35000
```

Запуск тестів:

```bash
pytest vrl_client/test_*.py -v
```

## 📝 Документація

- Оновіть README.md при змінах в API
- Додайте комментарі в коді для складних логік
- Структура директорій описана в README

## 🐛 Звіт про баги

Створіть issue з:
- Описанням проблеми
- Кроками для відтворення
- Очікуваним та фактичним результатом
- Версією Python та ОС

## 🎯 Напрямки розвитку

Приорітетні області:
- [ ] REST API документація
- [ ] WebSocket підтримка
- [ ] Linux/macOS деплоймент
- [ ] GUI інтерфейс
- [ ] Моніторинг дашборд

## 📞 Допомога

- Питання? Створіть **Discussion** в GitHub
- Проблеми? Створіть **Issue**
- Хочете поговорити? Email: [contact@example.com]

## ✨ Спасибо!

Кожна контрибуція допомагає зробити VRL Client краще. Дякуємо за участь! 🙏
