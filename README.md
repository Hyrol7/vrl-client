# VRL Client

Локальний клієнт для збору, аналізу та передачі телеметрії від Mode S/FLARM декодера на центральний сервер.

## 📋 Основні можливості

*   **Збір даних:** Підключення до декодера через TCP (порт 31003) та парсинг AVR пакетів (K1/K2).
*   **Аналіз та Кореляція:**
    *   **Frequency Analysis (K1):** Визначення достовірності (`faithfulness`) на основі частоти пакетів.
    *   **Time Correlation (K2):** Прив'язка "безхозних" пакетів висоти/палива до літаків на основі часових міток.
*   **Надійна синхронізація:**
    *   Використання UUID для ідентифікації кожного пакету.
    *   Двонаправлена синхронізація статусів (`Create` / `Update`).
    *   Захист від дублікатів та колізій даних.
*   **Безпека:** HMAC-SHA256 підпис всіх запитів до API.
*   **Автономність:** Локальна SQLite БД, автоматичне відновлення після збоїв, буферизація даних при відсутності інтернету.

## 🛠 Архітектура

Система складається з 5 асинхронних модулів:
1.  **Parser:** Читає TCP потік, парсить пакети, зберігає в БД (Status: `Pending Analysis` / `Ready to Send`).
2.  **Analyser:** Періодично аналізує накопичені дані, розраховує рейтинги довіри та корелює K1/K2 пакети.
3.  **Sender:** Відправляє нові (`Create`) та оновлені (`Update`) пакети на сервер.
4.  **Ping Handler:** Підтримує зв'язок з сервером ("heartbeat").
5.  **Status Manager:** Моніторить здоров'я системи та пише логи.

## 🚀 Швидкий старт

1.  Завантажте останній реліз `VRL_Client.exe`.
2.  Запустіть файл — автоматично створиться `config.yaml`.
3.  Налаштуйте `config.yaml` (вказати IP декодера та API ключі).
4.  Запустіть клієнт знову.

👉 **[Детальна інструкція з установки та налаштування](INSTALL.md)**

## ⚙️ Конфігурація

Основні налаштування знаходяться у `config.yaml`:
*   **decoder:** Параметри підключення до джерела даних.
*   **api:** URL сервера та ключі доступу.
*   **cycles:** Інтервали роботи аналізатора та сендера.

## 📄 Ліцензія

MIT License.
